# GitHub API token used for the contribution graph on the home page.
# Without it the panel still shows recent events + repos (REST is public),
# but the contribution graph needs auth. Create a token at
# https://github.com/settings/tokens (read-only, no scopes needed for public data).
NEXT_PUBLIC_GITHUB_TOKEN=
